import setuptools

setuptools.setup(
    name="jhoauthrefresh",
    version='0.2.0',
    url="https://github.com/OpenHumans/jhoauth-refresh",
    author="Tim Head",
    packages=setuptools.find_packages(),
    install_requires=[
        'notebook',
    ],
)
